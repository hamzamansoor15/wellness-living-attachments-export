# WellnessLiving credentials are prompted at runtime — do NOT put them here.
# This file exists as a placeholder if you need to override config values via env.

# Optional: override the download directory
# DOWNLOAD_DIR=./downloads

# Optional: override k_business if migrating a different account
# K_BUSINESS=643838
